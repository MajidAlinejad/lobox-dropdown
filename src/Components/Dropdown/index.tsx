import { FC } from "react";

interface IDropdownType {}

const Dropdown: FC<IDropdownType> = ({}) => {
	return (
		<>
			<select>
				<option>a</option>
				<option>a</option>
				<option>a</option>
				<option>a</option>
			</select>
		</>
	);
};

export default Dropdown;
